{
  "name": "@sge/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared ABIs, addresses, and constants for SGE Energy",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
